# Copilot Instructions

## Project Guidelines
- User requires that the game never present a zero-candidate cell: computeLiveCandidates must ensure at least one globally-valid candidate (use solution or countSolutions fallback) and preserve behavior where incorrect player entries do not remove candidates.